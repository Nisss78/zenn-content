---
title: "【2026年版】iOSの全機能カタログ - 面白アプリ開発のための完全ガイド"
emoji: "📱"
type: "tech"
topics: ["iOS", "Swift", "アプリ開発", "SwiftUI"]
published: false
---

# はじめに

iPhoneアプリ開発で使える機能、全部まとめました。画像付きでわかりやすく解説します。

> 📌 この記事では**iOS 17以降**で使える機能を網羅しています

---

# 1. 基礎的な機能（超重要！）

## 1-1. ピクチャーインピクチャー（PiP）

![PiPのイメージ](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/avfoundation_pictureInPicture_2x.png)

動画を見ながら他のアプリを操作できる機能。

**何ができる？**
- 動画が小さなウィンドウで画面隅に表示
- ユーザーは他のアプリを操作可能
- ウィンドウはドラッグで移動・リサイズ可能

**実装例:**
```swift
import AVKit

class ViewController: UIViewController {
    var playerViewController: AVPlayerViewController!
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // PiPを有効化
        playerViewController.allowsPictureInPicturePlayback = true
    }
}
```

**活用アイデア:**
- 動画学習アプリ
- ライブ配信視聴アプリ
- ミーティング録画再生アプリ

---

## 1-2. ライブアクティビティ

![ライブアクティビティ](https://docs-assets.developer.apple.com/uploaded-images/P8bF5xFQ/activityKit_activity_2x.png)

ロック画面・Dynamic Islandでリアルタイム更新される情報カード。

**何ができる？**
- 配達状況、タイマー、スポーツスコアなどを常に表示
- ユーザーがアプリを開かなくても情報を確認可能
- 最大8時間まで更新可能

**実装例:**
```swift
import ActivityKit

struct DeliveryAttributes: ActivityAttributes {
    public struct ContentState: Codable, Hashable {
        var status: String
        var estimatedTime: Date
    }
    var orderNumber: String
}

// 開始
let activity = try Activity.request(
    attributes: DeliveryAttributes(orderNumber: "12345"),
    content: .init(
        state: .init(status: "調理中", estimatedTime: Date().addingTimeInterval(1800)),
        staleDate: nil
    ),
    pushType: nil
)
```

**活用アイデア:**
- デリバリー追跡
- タクシー到着予定
- スポーツ試合経過

---

## 1-3. ウィジェット

![ウィジェット](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/widgetKit_widgets_2x.png)

ホーム画面・ロック画面に配置できる情報表示機能。

**何ができる？**
- アプリを開かずに情報を確認
- 小・中・大サイズ対応
- インタラクティブなボタン配置可能（iOS 17以降）

**実装例:**
```swift
import WidgetKit
import SwiftUI

struct MyWidget: Widget {
    var body: some WidgetConfiguration {
        StaticConfiguration(kind: "MyWidget", provider: TimelineProvider()) { entry in
            MyWidgetView(entry: entry)
        }
        .configurationDisplayName("マイウィジェット")
        .description("今日のタスクを表示")
        .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])
    }
}
```

**活用アイデア:**
- TODOリスト表示
- 今日の天気
- カロリー消費量

---

## 1-4. カメラ

![カメラ](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/avfcam_touches_2x.png)

写真・動画撮影機能へのアクセス。

**何ができる？**
- 写真撮影（タイマー・フラッシュ対応）
- 動画撮影（スローモーション・タイムラプス）
- QRコード読み取り
- 深度センサー活用（ポートレートモード）

**実装例:**
```swift
import SwiftUI
import PhotosUI

struct CameraView: View {
    @State private var selectedItem: PhotosPickerItem?
    
    var body: some View {
        PhotosPicker(selection: $selectedItem, matching: .images) {
            Text("写真を撮る")
        }
    }
}
```

**活用アイデア:**
- 書類スキャンアプリ
- レシピ記録アプリ
- 植物図鑑アプリ

---

## 1-5. GPS（位置情報）

![GPS](https://docs-assets.developer.apple.com/uploaded-images/RLcH628Q/coreLocation_region_2x.png)

ユーザーの現在地を取得。

**何ができる？**
- 現在地の緯度経度を取得
- 移動ルートの記録
- 特定エリアへの出入り検知（ジオフェンス）

**実装例:**
```swift
import CoreLocation

class LocationManager: NSObject, CLLocationManagerDelegate, ObservableObject {
    private let manager = CLLocationManager()
    @Published var location: CLLocation?
    
    override init() {
        super.init()
        manager.delegate = self
        manager.requestWhenInUseAuthorization()
        manager.startUpdatingLocation()
    }
    
    func locationManager(_ manager: CLLocationManager, didUpdateLocations locations: [CLLocation]) {
        location = locations.last
    }
}
```

**活用アイデア:**
- 散歩ルート記録
- 位置共有アプリ
- 観光ガイドアプリ

---

## 1-6. プッシュ通知

![プッシュ通知](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/userNotifications_2x.png)

ユーザーにメッセージを送る。

**何ができる？**
- 通知センターにメッセージ表示
- バッジ（アイコンの赤数字）
- サウンド再生
- 画像・動画・ボタン付き通知

**実装例:**
```swift
import UserNotifications

func requestNotification() {
    UNUserNotificationCenter.current().requestAuthorization(
        options: [.alert, .badge, .sound]
    ) { granted, error in
        if granted {
            print("通知許可された！")
        }
    }
}

func sendNotification() {
    let content = UNMutableNotificationContent()
    content.title = "リマインダー"
    content.body = "タスクの時間です！"
    content.sound = .default
    
    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: 5, repeats: false)
    let request = UNNotificationRequest(identifier: UUID().uuidString, content: content, trigger: trigger)
    
    UNUserNotificationCenter.current().add(request)
}
```

**活用アイデア:**
- 毎日の習慣リマインダー
- ニュース速報
- メッセージ通知

---

## 1-7. センサー

![センサー](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreMotion_activity_2x.png)

加速度・ジャイロ・気圧など様々なセンサーにアクセス。

**何ができる？**
- 歩数カウント
- 傾き検出
- 揺れ検出
- 高度測定

**実装例:**
```swift
import CoreMotion

class MotionManager: ObservableObject {
    private let motion = CMMotionManager()
    @Published var x: Double = 0
    @Published var y: Double = 0
    @Published var z: Double = 0
    
    init() {
        if motion.isAccelerometerAvailable {
            motion.accelerometerUpdateInterval = 0.1
            motion.startAccelerometerUpdates(to: .main) { data, error in
                if let data = data {
                    self.x = data.acceleration.x
                    self.y = data.acceleration.y
                    self.z = data.acceleration.z
                }
            }
        }
    }
}
```

**活用アイデア:**
- 水準器アプリ
- 睡眠検出
- 万歩計

---

## 1-8. 連絡先

![連絡先](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/contactsUI_select_2x.png)

電話帳データへのアクセス。

**何ができる？**
- 連絡先一覧の表示
- 新規連絡先の追加
- 電話・メールの発信

**実装例:**
```swift
import ContactsUI

struct ContactPicker: UIViewControllerRepresentable {
    func makeUIViewController(context: Context) -> CNContactPickerViewController {
        let picker = CNContactPickerViewController()
        picker.delegate = context.coordinator
        return picker
    }
    
    func updateUIViewController(_ uiViewController: CNContactPickerViewController, context: Context) {}
    
    func makeCoordinator() -> Coordinator {
        Coordinator()
    }
    
    class Coordinator: NSObject, CNContactPickerDelegate {
        func contactPicker(_ picker: CNContactPickerViewController, didSelect contact: CNContact) {
            print("選択された: \(contact.givenName)")
        }
    }
}
```

**活用アイデア:**
- グループメッセージアプリ
- 名刺管理アプリ
- 誕生日リマインダー

---

## 1-9. Face ID / Touch ID

![生体認証](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/localAuthentication_faceID_2x.png)

指紋・顔認証機能。

**何ができる？**
- ログイン認証
- 購入確認
- センシティブな情報へのアクセス制御

**実装例:**
```swift
import LocalAuthentication

func authenticate() {
    let context = LAContext()
    var error: NSError?
    
    if context.canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, error: &error) {
        context.evaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, localizedReason: "認証してください") { success, error in
            if success {
                print("認証成功！")
            }
        }
    }
}
```

**活用アイデア:**
- パスワード管理アプリ
- 秘密の日記アプリ
- 金融アプリ

---

# 2. 画面・UI系の機能

## 2-1. Dynamic Island

![Dynamic Island](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/activityKit_dynamicIsland_2x.png)

iPhone 14 Pro以降のインカメラ部分を活用したインタラクティブ領域。

**何ができる？**
- 通知をコンパクトに表示
- アプリの状態をリアルタイム表示
- タップで詳細展開

**特徴:**
- ライブアクティビティと連携
- コンパクト・最小・拡大の3モード

**活用アイデア:**
- タイマーアプリ
- 音楽プレイヤー
- スポーツスコア表示

---

## 2-2. Always-On Display

![常時表示](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/activityKit_alwaysOn_2x.png)

画面を消さずに情報を表示し続ける機能（iPhone 14 Pro以降）。

**何ができる？**
- ウィジェットを常に表示
- 時計・日付を常時表示
- バッテリー消費を抑えながら表示

**活用アイデア:**
- 時計・カレンダーアプリ
- タスク表示アプリ
- モチベーション引用句表示

---

## 2-3. Lock Screen Widgets

![ロック画面ウィジェット](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/widgetKit_lockScreen_2x.png)

ロック画面に配置できるコンパクトなウィジェット。

**何ができる？**
- ロック解除せずに情報確認
- 円形・四角形・横長の3サイズ
- ボタンでアプリ内アクション実行

**活用アイデア:**
- 今日の天気
- カレンダー次の予定
- バッテリー残量

---

## 2-4. App Clips

![App Clips](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/appClips_experience_2x.png)

インストール不要のミニアプリ。

**何ができる？**
- QRコード・NFCで即座に起動
- 10MB以下の軽量アプリ
- App Storeを経由せずに利用可能

**活用アイデア:**
- 駐車決済アプリ
- メニュー注文アプリ
- レンタルサイクル

---

## 2-5. Quick Actions

![Quick Actions](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/uikit_homeScreenActions_2x.png)

アプリアイコンを長押しで表示されるショートカット。

**何ができる？**
- アプリ内の特定機能に直接アクセス
- 3D Touch / Haptic Touch対応
- 最大4つのアクションを定義

**実装例:**
```swift
// Info.plist
<key>UIApplicationShortcutItems</key>
<array>
    <dict>
        <key>UIApplicationShortcutItemTitle</key>
        <string>新規作成</string>
        <key>UIApplicationShortcutItemType</key>
        <string>com.myapp.new</string>
    </dict>
</array>
```

**活用アイデア:**
- 「新規メモ作成」
- 「カメラを起動」
- 「検索画面へ」

---

# 3. メディア・AR系の機能

## 3-1. ARKit

![ARKit](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/arkit_worldTracking_2x.png)

拡張現実（AR）を実現するフレームワーク。

**何ができる？**
- 3Dオブジェクトを現実空間に配置
- 平面検出（床・テーブルなど）
- 顔認識・体の動き検出
- 空間のスキャン

**実装例:**
```swift
import ARKit
import RealityKit

struct ARViewContainer: UIViewRepresentable {
    func makeUIView(context: Context) -> ARView {
        let arView = ARView(frame: .zero)
        
        // 平面検出を開始
        let config = ARWorldTrackingConfiguration()
        config.planeDetection = .horizontal
        arView.session.run(config)
        
        // 3Dオブジェクトを配置
        let anchor = AnchorEntity(plane: .horizontal)
        let box = ModelEntity(mesh: MeshResource.generateBox(size: 0.1))
        anchor.addChild(box)
        arView.scene.addAnchor(anchor)
        
        return arView
    }
    
    func updateUIView(_ uiView: ARView, context: Context) {}
}
```

**活用アイデア:**
- 家具配置シミュレーター
- インリアルゴーストハント
- 測定アプリ

---

## 3-2. RealityKit

![RealityKit](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/realityKit_2x.png)

ハイエンド3Dコンテンツを簡単に扱えるフレームワーク。

**何ができる？**
- 3Dモデルの読み込み（USDZ形式）
- 物理シミュレーション
- アニメーション
- パーティクルエフェクト

**活用アイデア:**
- 3Dビューア
- ゲーム
- 製品プレビューアプリ

---

## 3-3. AVFoundation（動画編集）

![AVFoundation](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/avfoundation_edit_2x.png)

動画の撮影・編集・再生。

**何ができる？**
- 動画のトリミング・結合
- フィルター適用
- テキスト・画像のオーバーレイ
- 音声の合成

**活用アイデア:**
- 動画編集アプリ
- TikTok風ショート動画
- 動画水印追加ツール

---

## 3-4. Core Image

![Core Image](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreImage_filters_2x.png)

画像にフィルターを適用するフレームワーク。

**何ができる？**
- 200種類以上の組み込みフィルター
- 色調補正・ぼかし・シャープ化
- 顔検出

**実装例:**
```swift
import CoreImage

func applyFilter(to image: UIImage) -> UIImage {
    let ciImage = CIImage(image: image)
    let filter = CIFilter(name: "CISepiaTone")
    filter?.setValue(ciImage, forKey: kCIInputImageKey)
    filter?.setValue(0.8, forKey: kCIInputIntensityKey)
    
    let context = CIContext()
    let output = filter?.outputImage
    let cgImage = context.createCGImage(output!, from: output!.extent)
    
    return UIImage(cgImage: cgImage!)
}
```

**活用アイデア:**
- 写真編集アプリ
- インスタ風フィルター
- QRコード生成

---

## 3-5. Metal

![Metal](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/metal_shader_2x.png)

ハイエンドグラフィック描画API。

**何ができる？**
- 高性能3D描画
- ゲーム開発
- GPU計算

**活用アイデア:**
- 3Dゲーム
- CADアプリ
- 科学可視化

---

# 4. AI・機械学習系の機能

## 4-1. Core ML

![Core ML](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreML_2x.png)

機械学習モデルをアプリ内で実行。

**何ができる？**
- 画像分類・物体検出
- テキスト分類
- 数値予測
- オンデバイスで動作（プライバシー保護）

**実装例:**
```swift
import CoreML
import Vision

func classify(image: UIImage) {
    guard let model = try? VNCoreMLModel(for: MobileNetV2().model) else { return }
    
    let request = VNCoreMLRequest(model: model) { request, error in
        guard let results = request.results as? [VNClassificationObservation] else { return }
        print("判定結果: \(results.first!.identifier)")
    }
    
    let handler = VNImageRequestHandler(cgImage: image.cgImage!, options: [:])
    try? handler.perform([request])
}
```

**活用アイデア:**
- 植物図鑑
- 料理カロリー推定
- 感情分析カメラ

---

## 4-2. Create ML

![Create ML](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/createML_2x.png)

ノーコードで機械学習モデルを作成。

**何ができる？**
- 画像分類モデルを作成
- テキスト分類モデルを作成
- 推薦モデルを作成
- Xcode上で完結

**活用アイデア:**
- オリジナル画像分類器
- スパム検出
- 顧客セグメンテーション

---

## 4-3. Vision

![Vision](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/vision_faceDetection_2x.png)

画像認識の統合フレームワーク。

**何ができる？**
- 顔検出・顔認識
- テキスト認識（OCR）
- バーコード読み取り
- 物体検出・追跡

**実装例:**
```swift
import Vision

func detectText(in image: UIImage) {
    let request = VNRecognizeTextRequest { request, error in
        guard let observations = request.results as? [VNRecognizedTextObservation] else { return }
        
        for observation in observations {
            if let text = observation.topCandidates(1).first?.string {
                print("認識されたテキスト: \(text)")
            }
        }
    }
    
    let handler = VNImageRequestHandler(cgImage: image.cgImage!, options: [:])
    try? handler.perform([request])
}
```

**活用アイデア:**
- 名刺読み取り
- 書類デジタル化
- 視覚障害者支援アプリ

---

## 4-4. Natural Language

![Natural Language](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/naturalLanguage_2x.png)

テキストの自然言語処理。

**何ができる？**
- 言語判定
- トークン化（単語分解）
- 品詞タグ付け
- 感情分析

**実装例:**
```swift
import NaturalLanguage

func analyzeSentiment(text: String) {
    let tagger = NLTagger(tagSchemes: [.sentimentScore])
    tagger.string = text
    
    let (sentiment, _) = tagger.tag(at: text.startIndex, unit: .paragraph, scheme: .sentimentScore)
    
    if let score = Double(sentiment?.rawValue ?? "0") {
        if score > 0.5 {
            print("ポジティブ！")
        } else if score < -0.5 {
            print("ネガティブ...")
        } else {
            print("中立")
        }
    }
}
```

**活用アイデア:**
- 日記アプリの感情分析
- カスタマーレビュー分析
- メールの感情分類

---

## 4-5. Speech

![Speech](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/speech_recognition_2x.png)

音声認識・合成。

**何ができる？**
- リアルタイム音声認識
- オフライン対応
- 多言語対応

**実装例:**
```swift
import Speech

func startRecording() {
    let audioSession = AVAudioSession.sharedInstance()
    try? audioSession.setCategory(.record, mode: .measurement)
    try? audioSession.setActive(true)
    
    let recognizer = SFSpeechRecognizer(locale: Locale(identifier: "ja-JP"))
    let request = SFSpeechAudioBufferRecognitionRequest()
    
    let audioEngine = AVAudioEngine()
    let inputNode = audioEngine.inputNode
    
    recognizer?.recognitionTask(with: request) { result, error in
        if let result = result {
            print("認識結果: \(result.bestTranscription.formattedString)")
        }
    }
    
    let recordingFormat = inputNode.outputFormat(forBus: 0)
    inputNode.installTap(onBus: 0, bufferSize: 1024, format: recordingFormat) { buffer, _ in
        request.append(buffer)
    }
    
    audioEngine.prepare()
    try? audioEngine.start()
}
```

**活用アイデア:**
- 音声メモアプリ
- 会議議事録自動作成
- 音声コマンドアプリ

---

# 5. 位置・地図系の機能

## 5-1. MapKit

![MapKit](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/mapKit_2x.png)

Apple公式地図をアプリ内で表示。

**何ができる？**
- 地図表示・操作
- ピン・マーカー配置
- ルート検索
- 近隣検索

**実装例:**
```swift
import MapKit

struct MapView: View {
    @State private var region = MKCoordinateRegion(
        center: CLLocationCoordinate2D(latitude: 35.6812, longitude: 139.7671),
        span: MKCoordinateSpan(latitudeDelta: 0.1, longitudeDelta: 0.1)
    )
    
    var body: some View {
        Map(coordinateRegion: $region, annotationItems: [
            Location(name: "東京駅", coordinate: CLLocationCoordinate2D(latitude: 35.6812, longitude: 139.7671))
        ]) { location in
            MapMarker(coordinate: location.coordinate)
        }
    }
}
```

**活用アイデア:**
- 旅行ガイドアプリ
- 店舗検索アプリ
- ジオキャッシング

---

## 5-2. Core Location（詳細機能）

![ジオフェンス](https://docs-assets.developer.apple.com/uploaded-images/RLcH628Q/coreLocation_region_2x.png)

**高度な機能:**
- **ジオフェンス**: 特定エリアへの出入りを検知
- **ビーコン検知**: iBeacon信号の送受信
- **フォアグラウンド/バックグラウンド位置追跡**

**実装例（ジオフェンス）:**
```swift
func startGeofence() {
    let center = CLLocationCoordinate2D(latitude: 35.6812, longitude: 139.7671)
    let region = CLCircularRegion(center: center, radius: 100, identifier: "Tokyo Station")
    
    region.notifyOnEntry = true
    region.notifyOnExit = true
    
    manager.startMonitoring(for: region)
}

func locationManager(_ manager: CLLocationManager, didEnterRegion region: CLRegion) {
    print("エリアに入った！")
}
```

**活用アイデア:**
- 出退勤管理アプリ
- 安全区域通知（子供向け）
- トドロク観光スポット通知

---

## 5-3. iBeacon

![iBeacon](https://docs-assets.developer.apple.com/uploaded-images/RLcH628Q/coreLocation_beacon_2x.png)

近接ビーコンとの通信。

**何ができる？**
- 近くのビーコンを検出
- 距離推定（即時・近い・遠い）
- 店内ナビゲーション

**活用アイデア:**
- 博物館ガイド
- ショッピングモール案内
- イベントチェックイン

---

# 6. 体感・モーション系の機能

## 6-1. Core Motion

![Core Motion](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreMotion_activity_2x.png)

デバイスの動きを検出。

**何ができる？**
- 加速度・ジャイロ・コンパス
- 歩数取得
- 活動状態判定（歩行・走行・車・静止）

**実装例:**
```swift
import CoreMotion

class PedometerManager: ObservableObject {
    let pedometer = CMPedometer()
    @Published var steps: Int = 0
    
    init() {
        if CMPedometer.isStepCountingAvailable() {
            pedometer.startUpdates(from: Date()) { data, error in
                if let data = data {
                    DispatchQueue.main.async {
                        self.steps = data.numberOfSteps.intValue
                    }
                }
            }
        }
    }
}
```

**活用アイデア:**
- 万歩計
- 睡眠検出
- 振るジェスチャーゲーム

---

## 6-2. HealthKit

![HealthKit](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/healthKit_2x.png)

健康データへのアクセス。

**何ができる？**
- 心拍数・血圧・体温
- 歩数・走行距離・消費カロリー
- 睡眠データ
- ワクチン接種記録

**実装例:**
```swift
import HealthKit

class HealthManager {
    let healthStore = HKHealthStore()
    
    func requestAuthorization() {
        let types: Set = [HKQuantityType.quantityType(forIdentifier: .stepCount)!]
        
        healthStore.requestAuthorization(toShare: nil, read: types) { success, error in
            if success {
                print("HealthKit認証成功！")
            }
        }
    }
    
    func fetchSteps() {
        let stepType = HKQuantityType.quantityType(forIdentifier: .stepCount)!
        let query = HKStatisticsQuery(quantityType: stepType, quantitySamplePredicate: nil, options: .cumulativeSum) { _, result, _ in
            if let result = result, let sum = result.sumQuantity() {
                print("今日の歩数: \(sum.doubleValue(for: HKUnit.count()))")
            }
        }
        
        healthStore.execute(query)
    }
}
```

**活用アイデア:**
- 健康管理アプリ
- 睡眠トラッカー
- ダイエットアプリ

---

## 6-3. Haptic Feedback

![触覚フィードバック](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/uikit_haptics_2x.png)

振動でフィードバックを提供。

**何ができる？**
- 軽い振動・強い振動
- 成功・警告・エラーのパターン
- 選択時のフィードバック

**実装例:**
```swift
import UIKit

func tapHaptic() {
    let generator = UIImpactFeedbackGenerator(style: .medium)
    generator.impactOccurred()
}

func successHaptic() {
    let generator = UINotificationFeedbackGenerator()
    generator.notificationOccurred(.success)
}
```

**活用アイデア:**
- ボタン押下フィードバック
- ゲームのヒット感
- 入力成功・失敗の通知

---

# 7. 連携・通信系の機能

## 7-1. Core NFC

![NFC](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreNfc_tags_2x.png)

NFCタグの読み書き。

**何ができる？**
- NFCタグ読み取り
- NFCタグ書き込み
- Suica・PASMO残高確認（日本のみ）

**実装例:**
```swift
import CoreNFC

class NFCReader: NSObject, NFCNDEFReaderSessionDelegate, ObservableObject {
    var session: NFCNDEFReaderSession?
    
    func startReading() {
        session = NFCNDEFReaderSession(delegate: self, queue: nil, invalidateAfterFirstRead: true)
        session?.alertMessage = "NFCタグを近づけてください"
        session?.begin()
    }
    
    func readerSession(_ session: NFCNDEFReaderSession, didDetectNDEFs messages: [NFCNDEFMessage]) {
        for message in messages {
            for record in message.records {
                print("読み取り成功: \(record)")
            }
        }
    }
}
```

**活用アイデア:**
- 名刺NFC読み取り
- 在庫管理アプリ
- 認証カードリーダー

---

## 7-2. Core Bluetooth

![Bluetooth](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/coreBluetooth_2x.png)

Bluetooth機器との通信。

**何ができる？**
- BLE機器の検出・接続
- データ送受信
- ペリフェラル（周辺機器）として動作

**活用アイデア:**
- 心拍センサーアプリ
- スマートロック
- ウェアラブル連携

---

## 7-3. AirDrop / Share Sheet

![共有](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/uikit_sharing_2x.png)

コンテンツの共有。

**何ができる？**
- AirDropで近くの人に送信
- LINE・Twitterなどに共有
- ファイル・テキスト・URL共有

**実装例:**
```swift
import SwiftUI

struct ShareView: View {
    let items: [Any] = ["シェアするテキスト"]
    
    var body: some View {
        ShareLink(items: items) {
            Label("共有", systemImage: "square.and.arrow.up")
        }
    }
}
```

**活用アイデア:**
- 作成した画像をシェア
- スコアをSNSに投稿
- ファイル転送

---

## 7-4. Universal Links

![Universal Links](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/universalLinks_2x.png)

Webリンクからアプリを直接起動。

**何ができる？**
- https://リンクをタップでアプリ起動
- 特定画面へのディープリンク
- ブラウザ経由せずシームレス遷移

**活用アイデア:**
- メールマガジンから商品ページへ
- 広告からアプリ内キャンペーンへ
- 招待リンク

---

## 7-5. CarPlay

![CarPlay](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/carPlay_2x.png)

車載画面への対応。

**何ができる？**
- 車のインフォテインメントでアプリ使用
- 音楽・ナビ・オーディオブック対応
- 安全な運転画面設計

**活用アイデア:**
- 音楽ストリーミング
- オーディオブック
- 充電ステーション検索

---

# 8. データ・同期系の機能

## 8-1. CloudKit

![CloudKit](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/cloudKit_2x.png)

iCloud経由でデータを同期。

**何ができる？**
- データをクラウドに保存
- 全デバイスで自動同期
- パブリック・プライベートデータベース

**活用アイデア:**
- メモアプリの同期
- 設定の引き継ぎ
- ユーザープロフィール

---

## 8-2. SwiftData

![SwiftData](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/swiftData_2x.png)

iOS 17以降の最新データ永続化フレームワーク。

**何ができる？**
- モデル定義が簡単
- SwiftUIと完全統合
- 自動マイグレーション

**実装例:**
```swift
import SwiftData

@Model
class Task {
    var title: String
    var isCompleted: Bool
    var createdAt: Date
    
    init(title: String) {
        self.title = title
        self.isCompleted = false
        self.createdAt = Date()
    }
}

struct TaskListView: View {
    @Query var tasks: [Task]
    @Environment(\.modelContext) var context
    
    var body: some View {
        List(tasks) { task in
            Text(task.title)
        }
    }
}
```

**活用アイデア:**
- TODOアプリ
- 家計簿
- コンタクト管理

---

## 8-3. App Groups

![App Groups](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/appGroups_2x.png)

複数アプリ間でデータ共有。

**何ができる？**
- 本体アプリとウィジェット間でデータ共有
- App Clipと本体アプリ間でデータ共有
- 共有コンテナにアクセス

**活用アイデア:**
- ウィジェット用キャッシュ
- 複数アプリ間の設定同期

---

## 8-4. Keychain

![Keychain](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/keychain_2x.png)

暗号化された安全なデータ保存。

**何ができる？**
- パスワード保存
- トークン保存
- 暗号化が必要なデータ保護

**活用アイデア:**
- ログイン情報保存
- APIキー管理
- クレジットカード情報

---

# 9. セキュリティ・認証系の機能

## 9-1. Sign in with Apple

![Sign in with Apple](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/authenticationServices_signInApple_2x.png)

Apple IDでログイン。

**何ができる？**
- パスワードレス認証
- メールアドレス非公開オプション
- 生体認証との組み合わせ

**実装例:**
```swift
import AuthenticationServices

struct SignInWithAppleButton: View {
    var body: some View {
        SignInButton(
            .signIn,
            onRequest: { request in
                request.requestedScopes = [.email, .fullName]
            },
            onCompletion: { result in
                switch result {
                case .success(let authorization):
                    print("ログイン成功: \(authorization)")
                case .failure(let error):
                    print("ログイン失敗: \(error)")
                }
            }
        )
    }
}
```

**活用アイデア:**
- あらゆるアプリのログイン
- アカウント作成の手間を省く

---

## 9-2. DeviceCheck

![DeviceCheck](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/deviceCheck_2x.png)

デバイス認証で不正防止。

**何ができる？**
- デバイスごとの一意識別
- 不正利用検出
- トークン付与

**活用アイデア:**
- 無料トライアル制限
- 複数アカウント防止
- 詐害アプリ対策

---

## 9-3. App Attest

![App Attest](https://docs-assets.developer.apple.com/uploaded-images/bV1bV9QB/appAttest_2x.png)

アプリの改ざん検知。

**何ができる？**
- アプリが正規のものであることを証明
- リクエストの署名検証
- サーバー側での検証

**活用アイデア:**
- 金融アプリ
- ギャンブルアプリ
- 高額決済アプリ

---

# 10. 面白い組み合わせアイデア20選

## アイデア1: 感情日記カメラ 📸

**機能組み合わせ: Vision + Natural Language + HealthKit**

- 写真を撮る→Visionで表情分析
- 日記を書く→Natural Languageで感情分析
- HealthKitで心拍数と相関分析
- グラフでメンタル変化を可視化

---

## アイデア2: AR家具配置シミュレーター 🛋️

**機能組み合わせ: ARKit + Vision + Core ML**

- 部屋をスキャン→平面検出
- 家具カタログから選択
- ARで配置イメージ
- Visionでサイズ自動推定

---

## アイデア3: インリアルゴーストハント 👻

**機能組み合わせ: ARKit + Core Motion + Audio**

- ARでゴーストを配置
- Core Motionでスマホを傾けて探索
- 近づくとバイブレーション
- 音響で臨場感

---

## アイデア4: 位置連動詩人 🗺️

**機能組み合わせ: Core Location + Natural Language + Speech**

- 現在地の地名を取得
- その場所に関連する詩を生成
- Speechで朗読
- ジオフェンスで新しい場所に入ると自動再生

---

## アイデア5: 水平器職人 📐

**機能組み合わせ: Core Motion + ARKit + Haptic**

- 加速度で傾き検出
- ARで床に水平線表示
- 水平になると振動フィードバック
- 記録保存

---

## アイデア6: QRコード作曲家 🎵

**機能組み合わせ: Vision + AVFoundation + Core Image**

- QRコードで楽譜読み取り
- AVFoundationで音声生成
- Core Imageで楽譜画像生成
- MIDIエクスポート

---

## アイデア7: 声で動く絵本 📖

**機能組み合わせ: Speech + AVFoundation + SwiftUI**

- 音声でページめくり
- 読み上げるとキャラが動く
- BGM・効果音自動同期
- 子供向け教育アプリ

---

## アイデア8: 睡眠の質向上リラックス 😴

**機能組み合わせ: HealthKit + Core Motion + Audio**

- 睡眠データを分析
- 最適な寝る時間を通知
- リラックス音楽再生
- 加速度で寝返り検出

---

## アイデア9: 食べたものカロリー自動計算 🍱

**機能組み合わせ: Vision + Core ML + HealthKit**

- 写真を撮る→Visionで料理認識
- Core MLでカロリー推定
- HealthKitに記録
- ウィジェットで今日の摂取量表示

---

## アイデア10: スポーツ観戦ライブアクティビティ ⚽

**機能組み合わせ: Live Activity + Push Notification + Widget**

- 試合スコアをリアルタイム表示
- ゴール時プッシュ通知
- ロック画面で経過確認
- Dynamic Island対応

---

## アイデア11: インリアル測定アプリ 📏

**機能組み合わせ: ARKit + Vision + RealityKit**

- 物体をARで認識
- 自動でサイズ測定
- 3Dモデルとして保存
- ルームスキャン

---

## アイデア12: NFC認証出退勤管理 🏢

**機能組み合わせ: Core NFC + Core Location + CloudKit**

- NFCタグでタッチ
- GPSで位置確認
- CloudKitで同期
- 管理者用ダッシュボード

---

## アイデア13: 万歩計宝探しゲーム 🚶

**機能組み合わせ: Core Motion + MapKit + GameKit**

- 歩数でポイント獲得
- 地図上に宝を配置
- 到達でアイテムゲット
- ランキング機能

---

## アイデア14: 手話翻訳アプリ 🤟

**機能組み合わせ: Vision + Core ML + Speech**

- カメラで手話認識
- テキスト・音声に変換
- 双方向コミュニケーション
- 旅行時支援

---

## アイデア15: カフェノイズ生成機 ☕

**機能組み合わせ: AVFoundation + Haptic + Widget**

- カフェ・雨・焚き火などの環境音
- 振動でランチタイム通知
- ウィジェットで素早く起動
- タイマー機能

---

## アイデア16: NFC名刺交換アプリ 👔

**機能組み合わせ: Core NFC + Contacts + Share Sheet**

- NFCタグで名刺データ読み取り
- 連絡先に自動保存
- AirDropで交換
- プロフィール画像生成

---

## アイデア17: AR植物図鑑 🌿

**機能組み合わせ: ARKit + Vision + Core ML**

- 植物をARで認識
- 名前・育て方表示
- 3Dモデルで生長シミュレーション
- 写真保存

---

## アイデア18: 動画字幕自動生成 🎬

**機能組み合わせ: Speech + Vision + AVFoundation**

- 音声をテキスト化
- 字幕を動画に焼き込み
- 多言語翻訳
- SRTエクスポート

---

## アイデア19: 位置連動TODO 📍

**機能組み合わせ: Core Location + Geofence + Notification**

- 特定場所でタスク通知
- スーパーに着くと買い物リスト表示
- 家に着くと掃除リマインダー
- 自動完了機能

---

## アイデア20: HealthKit + Widget で健康ダッシュボード 💪

**機能組み合わせ: HealthKit + Widget + Charts**

- 歩数・心拍・睡眠を統合
- ウィジェットで今日のサマリー
- トレンドグラフ
- 目標達成通知

---

# まとめ

iOSアプリ開発で使える機能は本当に多い！この記事で紹介したのは一部だけど、これらを組み合わせるだけでアイデアは無限に広がります。

**開発のコツ:**
1. まずは1つの機能を深掘り
2. 小さなアプリで試す
3. 組み合わせて独自性を出す
4. ユーザー体験を最優先

**リソース:**
- [Apple公式ドキュメント](https://developer.apple.com/documentation/)
- [Hacking with Swift](https://www.hackingwithswift.com/)
- [SwiftUI チュートリアル](https://developer.apple.com/tutorials/swiftui)

---

**次のステップ:**
- 気になる機能を選んで、公式ドキュメントを読む
- サンプルプロジェクトをcloneして動かす
- 自分のアプリに組み込んでみる

何か作ったら、ぜひシェアしてください！🎉

---

**参考文献:**
- [Apple Developer Documentation](https://developer.apple.com/documentation/)
- [iOS App Architecture](https://developer.apple.com/documentation/swiftui/managing-user-interface-state)
- [Core Data Programming Guide](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/CoreData/)
